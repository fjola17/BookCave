@model List<BookCave.Models.ViewModels.BookViewModel>


  <body class="background">
    <div class="content">
      <h2>New Releases</h2>
        <table id="newBooks">
        @foreach(var b in Model)
        {
          <td><a href="#"><img src="@b.CoverImage" alt="CoverImage" height="170px" width="auto" /><a/></td>
          <td><a asp-controller="Book" asp-action="Details" asp-route-id="@b.Id" >@b.Title</a></td>
          <td>@b.Price</td>
        }
        </tr>
      
      </table>
      
    </div>
  </body>